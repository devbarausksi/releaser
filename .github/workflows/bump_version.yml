name: Create Release

on:
  push:
    branches:
      - main

jobs:
  runner:
    name: Runner
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-node@v4

      - name: Check files and get content
        run: |
          FILES=$(ls .changeset/pr-*.md)

          if [ ${#FILES[@]} -eq 0 ]; then
            echo "No changeset files found"
            exit 1
          fi

          if [ ${#FILES[@]} -gt 1 ]; then
            echo "More than one changeset file found"
            exit 1
          fi

          echo "RELEASE_FILE=$FILES[0]" >> $GITHUB_ENV
          echo "RELEASE_CONTENT=$(cat $RELEASE_FILE)" >> $GITHUB_ENV

      - name: Copy changelogs
        run: |
          if [ ! -d "releases" ]; then
            mkdir releases
          fi
          cp .changeset/${{ env.RELEASE_FILE }} releases/${{ env.RELEASE_FILE }}
      
      - name: Bump up version
        run: |
          npm install --omit=dev
          npx changeset version
          npx changeset tag

      - name: Commit and push markdown file to current branch
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Bump up version"
          git push origin main --follow-tags

      - uses: ncipollo/release-action@v1
        with:
          tag: $( git tag --sort=taggerdate | tail -1 )
          body: ${{ env.RELEASE_CONTENT }}

      